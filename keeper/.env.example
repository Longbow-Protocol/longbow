# Longbow keeper configuration. Copy to `.env` and fill in.

# Keeper account private key (receives liquidation bounties). Keep this secret.
PRIVATE_KEY=0x...

# Deployed PositionManager on Robinhood Chain (4663).
POSITION_MANAGER=0x326B88686F1c3d875e8aCC5A841658561c6baA65

# Optional overrides (defaults shown).
RPC_URL=https://rpc.mainnet.chain.robinhood.com
CHAIN_ID=4663
POLL_INTERVAL_MS=60000
